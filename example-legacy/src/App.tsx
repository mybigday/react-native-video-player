// The legacy-architecture example runs the same demo screen as the mobile
// example, so the Paper and Fabric code paths are exercised by identical UI.
export { default } from '../../example/src/App';
