// The Apple TV / Android TV example runs the same demo screen as the mobile
// example, so both stay in sync and the TV build stays a pure platform check.
export { default } from '../../example/src/App';
