nodeLinker: node-modules

# Keep each workspace's dependencies inside its own `node_modules`. React
# Native's Gradle and CocoaPods setup resolves paths such as
# `example/node_modules/react-native` literally, so hoisting them to the repo
# root breaks the native builds.
nmHoistingLimits: workspaces

enableGlobalCache: true
